// RUTA: src/app/diseno/vista/specialist/dashboard/page.tsx
// Banco de pruebas: la página REAL /specialist/dashboard con fetch simulado (fixtures) y el
// AppShell de su rol. El layout de /diseno/vista instala el simulador antes.
import PaginaReal from '@/app/specialist/dashboard/page';

export default function Vista() {
  return <PaginaReal />;
}

// RUTA: src/app/diseno/vista/company/interviews/page.tsx
// Banco de pruebas: la página REAL /company/interviews con fetch simulado (fixtures) y el
// AppShell de su rol. El layout de /diseno/vista instala el simulador antes.
import PaginaReal from '@/app/company/interviews/page';

export default function Vista() {
  return <PaginaReal />;
}
